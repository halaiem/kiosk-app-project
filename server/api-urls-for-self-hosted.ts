/**
 * Замени содержимое объекта URLS в файле src/api/driverApi.ts на эти строки
 * при деплое на свой сервер:
 */
const URLS = {
  auth: '/api/driver-auth',
  messages: '/api/driver-messages',
  manage: '/api/driver-manage',
};
